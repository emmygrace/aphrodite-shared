/**
 * @gaia-tools/aphrodite-shared
 * 
 * Platform-agnostic wheel definitions, visual/glyph configurations, and chart presets
 * for the Aphrodite chart rendering system.
 * 
 * This package can be used by:
 * - aphrodite-react (React bindings)
 * - aphrodite-python (future Python bindings)
 * - aphrodite-android (future Android bindings)
 * - Any other platform that needs chart configuration
 */

// Export wheel definitions and types
export * from './wheels';

// Export visual and glyph configurations
export * from './configs';

// Export chart presets
export * from './presets';

// Export all types
export * from './types';

// Export orientation types and presets
export * from './orientation';

